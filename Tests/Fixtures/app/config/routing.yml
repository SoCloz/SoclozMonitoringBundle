test_controller_exception:
  pattern: /socloz_monitoring/exception/{class}
  defaults: { _controller: SoclozMonitoringTestBundle:Test:generateException }

test_controller_timing:
  pattern: /socloz_monitoring/timing/{time}
  defaults: { _controller: SoclozMonitoringTestBundle:Test:timing }

test_controller_preg:
  pattern: /socloz_monitoring/preg/{count}
  defaults: { _controller: SoclozMonitoringTestBundle:Test:preg }

test_controller_request_id:
  pattern: /socloz_monitoring/request_id
  defaults: { _controller: SoclozMonitoringTestBundle:Test:requestId }
